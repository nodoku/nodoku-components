import type {IconType} from "react-icons";

export async function nameToReactIcon_{{module}}_{{fileIndex}}(iconName: string): Promise<IconType | undefined> {
    {{#icons}}
    //if (iconName === "{{module}}:{{.}}") {
    //    const { {{.}} } = await import("react-icons/{{module}}")
    //    return {{.}};
    //}
    {{/icons}}
}
